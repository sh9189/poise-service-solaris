require 'spec_helper'

describe Poise::Service::Solaris do
  it 'has a version number' do
    expect(Poise::Service::Solaris::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
